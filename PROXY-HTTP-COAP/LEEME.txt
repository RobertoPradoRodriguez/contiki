En la imagen 1 vemos como el proxy se ejecuta en el eclipse y se 
pone a escuchar peticiones HTTP en el puerto 8080. Válido para
hacer pruebas.

La carpeta pruebas es la importante, mirar primero y leer
su LEEME.txt.

La otra carpeta son pruebas alternativas.
